import { StyleSheet } from "react-native";
import { DARKEST_CYAN } from "../../../styles/colors";

const sideHeaderStyles = StyleSheet.create({
  headerContainer: {
    backgroundColor: DARKEST_CYAN,
    alignItems: "center"
  }
});

export default sideHeaderStyles;
