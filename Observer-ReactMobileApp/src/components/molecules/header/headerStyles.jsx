import { StyleSheet } from "react-native";
import { DARK_CYAN } from "../../../styles/colors";

const headerStyles = StyleSheet.create({
  headerContainer: {
    backgroundColor: DARK_CYAN,
    justifyContent: "center"
  }
});

export default headerStyles;
